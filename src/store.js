const notes = []
const contacts = []
const projects = []
const currentIds = {
    notes: 0,
    contacts: 0,
    projects: 0
}

export default {
    notes,
    contacts,
    projects,
    currentIds
}